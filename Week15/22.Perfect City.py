'''Bangkok's city plan suck'''
from math import ceil, floor
def san_franscisco():
    '''California'''
    point = [[float(input()), float(input())] for _ in range(2)]
    pickup, destination = point[0], point[1]
    block, road = [], []


        
def calculate(point_one, point_two):
    
    return abs(point_one[1] - point_two[0]) + abs(point_one[0] - point_two[0])


san_franscisco()
